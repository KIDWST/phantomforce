PhantomForce Install Notice

By installing PhantomForce, you agree that PhantomForce may install and update its local runtime, AI operations services, media helpers, PhantomPlay browser runtime support, and update components inside PhantomForce-owned application data folders.

These components are presented as PhantomForce because they operate as part of the PhantomForce product. Third-party notices and licenses remain available in the app/legal bundle and install records.

PhantomForce must not install components under misleading operating-system names, impersonate system files, or hide privileged activity. Operating-system permission prompts, external sending/posting/uploading, and destructive changes remain approval-gated.

Install manifest version:
phantomforce-install-consent-2026-07-12

# Phantom Unreal Recommendations

Next three priorities, in order:

1. Replace flat dominant surfaces and add camera-visible foreground detail per game, starting with Legends and CubeTown; rerun only the affected target and reject regressions.
2. Remove every false-semantic asset alias and the Legends egg-like silhouettes; convert required-art fallbacks into explicit validation failures.
3. After 4/4 visual passes, collect per-game frame-time/FPS/GPU/memory telemetry and a soak run before asking for human promotion review.

Do not spend time on promotion tooling or live-build replacement while any game remains below the visual bar.

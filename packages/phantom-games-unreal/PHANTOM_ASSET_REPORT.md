# PHANTOM ASSET REPORT

Installed asset acquisition manifest: `Config\PhantomAssetManifest.json`.

Installed orchestration tool: `Tools\Acquire-PhantomAssets.ps1`.

The package preserves the existing generated GLB and external CC0 fallback pipeline. The Fab tool uses the official signed-in Fab/Epic/Unreal workflow, never reads credentials/cookies/tokens, and never purchases paid content automatically. After local acquisition/import, rerun it with `-AuditOnly` to inventory project-side Fab/Megascans content.

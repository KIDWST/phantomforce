# Golden Path traces

Runtime traces are generated under `tmp/golden-path/<timestamp>/`. They contain sanitized IDs, states, and assertions only. Credentials, authorization headers, cookies, and provider secrets are never written.

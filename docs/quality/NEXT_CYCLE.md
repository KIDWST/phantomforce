# Next Evolution Cycle

Last updated: 2026-07-19 08:20 America/Chicago

## Start Here

Continue the finite scheduled task from `EVOLUTION_STATE.json`. This is not an
active Codex goal. Run `ops/evolution/Invoke-SevenDayEvolutionPreflight.ps1`,
fetch `origin/main`, and preserve concurrent commits before editing.

## One Priority

Prove and tighten the compact-width operating shell on both real app hosts:

1. Exercise authenticated admin and customer views at 320px, 390px, tablet,
   half-screen desktop, and full desktop.
2. Verify the compact header, Phantom chat composer, software keyboard focus,
   reduced motion, and drawer open/close behavior.
3. Confirm the admin drawer retains Admin, Developer, Away Mode, PhantomPlay,
   and every internal route while the customer drawer exposes only permitted
   organization routes.
4. Fix only reproduced overflow, occlusion, focus, or authority defects. Keep
   one registry and the existing permission/entitlement sources.
5. Capture before/after evidence, run the responsive matrix and release gate,
   push, sync the served checkout, and run the strict live-source doctor.

## Regression Commands

- `node scripts/test-responsive-viewports.mjs`
- `npm run test:release-critical`
- `npm run test:change-memory`
- `git diff --check`

## Stop Condition

Stop after one coherent, browser-proven batch. Record the stable/live commit,
remaining risk, and one next priority. The Windows task disables itself after
cycle 21; do not create an active goal or recurring work beyond that boundary.

# Next Quality Cycle

Last updated: 2026-07-19

## Start Here

Read `AGENTS.md` and every file under `docs/quality` before changing code. Do
not restart the inventory unless it is invalid. Continue in this Codex task via
heartbeat automation `continue-phantomforce-quality-program`.

Cycle 28 resolves explicit `respectively` mappings and named-person rollback
without model invention. Named, reverse, and ordinal callbacks return their
stated pair; unequal lists clarify rather than guess; and undoing Mina preserves
Theo's accepted change. A real Chrome journey proved 73 turns across two
isolated organizations, and the canonical gate passed 140 requests with zero
fallback or leakage. Deployment and scheduled-sync evidence is recorded in
`AUDIT_LOG.md`.

## Recommended Cycle 29

Theme: comparative facts, ordered events, and scoped corrections.

1. Exercise exact comparisons such as who has more, how many more, and ranking
   three named values without losing units or ownership.
2. Test ordered event callbacks such as what happened immediately before or
   after a named event, including repeated event types.
3. Correct one person's numeric value and verify comparative answers use the
   newest value while every other person's state remains unchanged.
4. Make ambiguous units, ties, or missing values produce one concise
   clarification instead of arithmetic invention.
5. Keep extraction and arithmetic bounded; leave synthesis and judgment on the
   lightest capable local model.
6. Run at least 100 real-model requests and the authenticated 390x844/1440x900
   browser journey, preserving corrections in change memory.

Likely files:

- `server/src/phantom-ai/instant-chat-context.ts`
- `server/src/phantom-ai/instant-chat-tools.ts`
- `server/scripts/test-instant-chat-http-live-model.ts`
- `scripts/test-database-auth-org-browser.mjs`

## Regression Commands To Keep

- `npm run test:instant-chat:http-live-model --workspace @phantomforce/server`
- `npm run test:database-auth`
- `npm run test:dashboard-chat`
- `node scripts/test-memory-retention.mjs`
- `npm run test:release-critical`
- `npm run test:change-memory`
- `git diff --check`

## Stop Condition

Stop after one coherent implemented and browser-verified batch. The owner has
authorized commit, push, and live deployment. Fetch and rebase `origin/main`,
preserve concurrent work, sync the dedicated deployment, and run the strict
live-source doctor before reporting the batch live.

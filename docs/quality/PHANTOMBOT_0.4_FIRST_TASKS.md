# PhantomBot 0.4 first-task evidence

Date: 2026-07-26

## Truth boundary

These tasks exercised the real PhantomForce governed executor against
disposable repositories and the source-backed Hermes planning context. They
did not produce a successful live-model Hermes turn: the installed provider
turn timed out during Phase 2. The real Termina dispatch remains blocked by
the two credential prerequisites recorded in
`PHANTOMBOT_0.4_REAL_TERMINA_INTEGRATION.md`.

## Task A — read-only orientation

The ecosystem knowledge test asked the required canonical/deployed,
Hermes/PhantomForce ownership, provider, and Termina relationship questions.
It verified:

- canonical repository `github.com/KIDWST/phantomforce`;
- inspected deployment
  `C:\Users\jorda\Documents\Codex\deployments\phantomforce-live`;
- the current checkout is a stabilization worktree, not the deployment;
- Hermes plans while PhantomForce validates, approves, executes, receipts,
  and persists memory;
- Termina is a separate approval-bound mission service.

Command:
`npm run test:hermes-ecosystem-knowledge --workspace @phantomforce/server`

## Task B — harmless documentation journey

The governed operator journey used a disposable canonical-repository fixture,
an exact documentation replacement, explicit approval, the real desktop test
command contract, diff verification, receipt creation, memory creation,
restart recovery, reopen, and duplicate suppression.

Latest verified receipt: `receipt-run-ms2fzggb-y50hdt`

Latest verified memory: `brain-memory-5804fc48-1011-4fe5-9e32-9ed345a79722`

The real product document was not mutated merely to manufacture release
evidence.

## Task C — controlled source change

The engineering-tools journey performed an exact approved fixture edit,
executed its matching allowlisted verification, inspected the result, and
created verified receipt `receipt-run-ms2fzjuj-9k0rpa`.

Approval granted for another file, encoded traversal, mixed separators,
junction escape, command mutation, malicious arguments, and secret-shaped
write content were rejected.

## Task D — failure and rollback

An approved fixture edit intentionally failed verification. The executor
restored prior content, attached rollback evidence, produced failure/rollback
receipt `receipt-run-ms2fzkcm-vndkvj`, and did not write success memory.

The operator journey separately verified pending and completed restart
recovery, completed-session reopen, and duplicate-execution prevention.

## Task E — real Termina

Not claimed complete. A real local Termina service was authenticated and
enumerated repositories, and the PhantomForce adapter reached it. Approved
dispatch then failed before mission creation because the hard-coded Claude
CLI decomposer's OAuth was expired. A shared `TERMINA_TOKEN` also still needs
secret-safe provisioning for the production pair. No token was invented or
recorded.
